/* ═══════════════════════════════════════════════════════
   certificates.css — V2 Certificate page
   Structural layout via blueprint.css internal-page system
   This file: card-specific, lightbox, and responsive overrides
   ═══════════════════════════════════════════════════════ */

/* ── Certificate Gallery Grid ── */
.ph-cert-gallery {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1.25rem;
}

/* ── Gallery description ── */
.ph-cert-gallery-desc { text-align:center; color:var(--ph-text-secondary); font-size:.9375rem; margin-bottom:1.75rem; }

/* ── CTA subtitle ── */
.ph-cert-cta-sub { margin-top:.85rem; color:var(--ph-text-muted); font-size:.875rem; }
.ph-cert-cta-sub a { color:var(--ph-accent); font-weight:500; }

/* ── Card — framed document presentation ── */
.ph-cert-card {
  background: var(--ph-surface-primary);
  border: 1px solid var(--ph-border-default);
  border-radius: var(--ph-radius-lg);
  padding: 1rem;
  display: flex;
  flex-direction: column;
  cursor: pointer;
  transition: transform var(--ph-duration-normal) var(--ph-ease-spring),
              border-color var(--ph-duration-fast) var(--ph-ease-out),
              box-shadow var(--ph-duration-normal) var(--ph-ease-out);
}
.ph-cert-card:hover {
  transform: translateY(-3px);
  border-color: var(--ph-accent);
  box-shadow: var(--ph-shadow-3);
}
.ph-cert-card:focus-visible {
  outline: 2px solid var(--ph-accent);
  outline-offset: 2px;
  border-color: var(--ph-accent);
}

/* ── Thumbnail — framed document look ── */
.ph-cert-thumb {
  position: relative;
  width: 100%;
  aspect-ratio: 3 / 4;
  border-radius: var(--ph-radius-md);
  overflow: hidden;
  margin-bottom: 0.75rem;
  background: var(--ph-bg-secondary);
  border: 1px solid var(--ph-border-subtle);
  box-shadow: inset 0 0 0 3px rgba(255,255,255,0.03);
}
.ph-cert-thumb img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  display: block;
  transition: transform var(--ph-duration-slow) var(--ph-ease-out);
}
.ph-cert-card:hover .ph-cert-thumb img { transform: scale(1.03); }

/* Zoom indicator */
.ph-cert-thumb::after {
  content: '';
  position: absolute;
  right: 8px; bottom: 8px;
  width: 32px; height: 32px;
  display: flex; align-items: center; justify-content: center;
  font-size: 14px;
  border-radius: 50%;
  background: rgba(11, 25, 41, 0.85);
  border: 1px solid var(--ph-border-active);
  opacity: 0;
  transform: translateY(4px);
  transition: opacity var(--ph-duration-fast) var(--ph-ease-out),
              transform var(--ph-duration-fast) var(--ph-ease-out);
  pointer-events: none;
}
.ph-cert-thumb:hover::after {
  opacity: 1;
  transform: translateY(0);
}

/* ── Card info ── */
.ph-cert-info { flex: 1; padding-top: 0.25rem; }
.ph-cert-info h3 {
  font: var(--ph-text-h4);
  color: var(--ph-text-primary);
  margin-bottom: 0.25rem;
  font-size: 0.9375rem;
}
.ph-cert-info p {
  font-size: 0.8125rem;
  color: var(--ph-text-secondary);
  line-height: 1.5;
}

/* ── Lightbox ── */
.ph-lightbox {
  position: fixed; inset: 0; z-index: 1000;
  display: none;
  align-items: center; justify-content: center;
  padding: 2.5rem 1.5rem;
  background: rgba(2, 13, 23, 0.94);
  backdrop-filter: blur(10px);
}
.ph-lightbox.open { display: flex; }
.ph-lightbox-content {
  position: relative;
  max-width: 900px; width: 100%; max-height: 100%;
  display: flex; flex-direction: column; align-items: center; gap: 0.75rem;
}
.ph-lightbox-img {
  max-width: 100%; max-height: 80vh;
  object-fit: contain;
  border-radius: var(--ph-radius-md);
  background: #fff;
  box-shadow: 0 24px 64px rgba(0, 0, 0, 0.65);
}
.ph-lightbox-caption {
  color: var(--ph-text-primary);
  font-size: 1.0625rem; font-weight: 600;
  text-align: center;
}
.ph-lightbox-counter {
  color: var(--ph-text-muted);
  font-size: 0.75rem; text-align: center;
  font-family: var(--ph-font-mono);
}

/* Lightbox controls */
.ph-lightbox-close {
  position: absolute; top: -2.5rem; right: 0;
  width: 40px; height: 40px; border-radius: 50%;
  border: 1px solid rgba(237, 241, 247, 0.12);
  background: rgba(11, 25, 41, 0.9);
  color: var(--ph-text-primary);
  font-size: 1.25rem; line-height: 1;
  cursor: pointer;
  transition: all var(--ph-duration-fast) var(--ph-ease-out);
  display: flex; align-items: center; justify-content: center;
}
.ph-lightbox-close:hover { background: var(--ph-danger); border-color: var(--ph-danger); color: #fff; }

.ph-lightbox-nav {
  position: absolute; top: 50%; transform: translateY(-50%);
  width: 44px; height: 44px; border-radius: 50%;
  border: 1px solid rgba(237, 241, 247, 0.12);
  background: rgba(11, 25, 41, 0.9);
  color: var(--ph-text-primary);
  font-size: 1.375rem; line-height: 1;
  cursor: pointer;
  transition: all var(--ph-duration-fast) var(--ph-ease-out);
  display: flex; align-items: center; justify-content: center;
}
.ph-lightbox-nav:hover { background: var(--ph-accent-subtle); border-color: var(--ph-accent); color: var(--ph-accent); }
.ph-lightbox-nav:focus-visible { outline: 2px solid var(--ph-accent); outline-offset: 2px; }
.ph-lightbox-prev { left: 0; }
.ph-lightbox-next { right: 0; }

/* ── Quality Process (vertical, numbered) ── */
.ph-quality-process {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.ph-quality-process .ph-quality-item {
  display: flex;
  gap: 1.25rem;
  align-items: flex-start;
  padding: 1.25rem;
  background: var(--ph-surface-primary);
  border: 1px solid var(--ph-border-default);
  border-radius: var(--ph-radius-md);
  transition: border-color var(--ph-duration-fast);
}
.ph-quality-process .ph-quality-item:hover { border-color: var(--ph-accent); }
.ph-quality-num {
  flex-shrink: 0;
  width: 40px; height: 40px;
  border-radius: 50%;
  background: var(--ph-accent-subtle);
  color: var(--ph-accent);
  display: flex; align-items: center; justify-content: center;
  font-weight: 700; font-size: 0.875rem;
  font-family: var(--ph-font-mono);
}
.ph-quality-body h3 {
  font: var(--ph-text-h4);
  color: var(--ph-text-primary);
  margin-bottom: 0.2rem;
  font-size: 0.9375rem;
}
.ph-quality-body p {
  font-size: 0.875rem;
  color: var(--ph-text-secondary);
  line-height: 1.55;
  margin: 0;
}

/* ── Responsive ── */
@media (max-width: 1024px) {
  .ph-cert-gallery { grid-template-columns: repeat(2, minmax(0, 1fr)); }
}

@media (max-width: 768px) {
  .ph-cert-gallery { grid-template-columns: 1fr; max-width: 440px; margin-left: auto; margin-right: auto; }
  .ph-quality-process .ph-quality-item { flex-direction: column; gap: 0.75rem; text-align: center; }
  .ph-quality-num { margin: 0 auto; }
  .ph-lightbox { padding: 1.5rem 1rem; }
  .ph-lightbox-nav { width: 38px; height: 38px; font-size: 1.125rem; }
  .ph-lightbox-prev { left: 0.125rem; }
  .ph-lightbox-next { right: 0.125rem; }
  .ph-lightbox-close { top: -2.25rem; right: 0.125rem; }
}

@media (max-width: 480px) {
  .ph-cert-gallery { max-width: 100%; }
}
